const baseUrl = "http://localhost:8080/api";

export const apiUrl  = {
    // Students
    studentUrl : `${baseUrl}/demo/students`,
    authenticateUrl : `${baseUrl}/auth/signin`,
    gradeUrl : `${baseUrl}/demo/grades`,
    classUrl : `${baseUrl}/demo/classrooms`,

    
}